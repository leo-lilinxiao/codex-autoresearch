interface:
  display_name: "Codex Autoresearch"
  short_description: "Single-entry autonomous experimentation with foreground or background runs"
  brand_color: "#2563EB"
  default_prompt: "Use $codex-autoresearch to scan the repo, confirm the target, then run logged foreground or background experiments."

policy:
  allow_implicit_invocation: false
